# disable faulty repos
---
- hosts: all
  become: yes
  gather_facts: yes
  remote_user: ansible

  tasks:
  - name: disable broken ansible_mbis repos
    ansible.builtin.replace:
      path: /etc/yum.repos.d/ansible_repos.repo
      regexp: 'enabled = 1'
      replace: 'enabled = 0'

